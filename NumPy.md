# 🔷 NumPyとは（簡単）

**NumPy（Numerical Python）** は、Pythonで「大量の数値計算を速く・簡単にするためのライブラリ」です。

特に **配列（Array）** を効率よく扱うことができ、AI、データ分析、画像処理、シミュレーションなど幅広い分野で利用されています。

---

# 🔷 NumPyでできること

- 配列（数のまとまり）を扱える
- 足し算・掛け算などを一括で計算できる
- 行列・ベクトル計算ができる
- 統計計算（平均・最大値・最小値など）ができる
- AI・データ分析・車載システム開発で活用される

## 例：配列の作成

```python
import numpy as np

a = np.array([10, 20, 30, 40])

print(a)
```

出力

```text
[10 20 30 40]
```

---

## 例：配列同士の計算

```python
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

print(a + b)
print(a * b)
```

出力

```text
[5 7 9]
[ 4 10 18]
```

---

## 例：行列計算

```python
A = np.array([
    [1, 2],
    [3, 4]
])

B = np.array([
    [5, 6],
    [7, 8]
])

print(A @ B)
```

出力

```text
[[19 22]
 [43 50]]
```

---

## 例：統計計算

```python
data = np.array([10, 20, 30, 40, 50])

print(np.mean(data))
print(np.max(data))
print(np.min(data))
```

出力

```text
30.0
50
10
```

---

# 🔷 NumPyが速い理由

## ① ベクトル化（Vectorization）

配列全体をまとめて計算する仕組みです。

### ❌ 普通のPython

```python
a = [1, 2, 3]
b = [4, 5, 6]

c = []

for i in range(len(a)):
    c.append(a[i] * b[i])

print(c)
```

出力

```text
[4, 10, 18]
```

### ✅ NumPy

```python
import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

c = a * b

print(c)
```

出力

```text
[ 4 10 18]
```

### メリット

- for文不要
- コードが短い
- 数学の式に近い
- 実行速度が速い
- バグが減りやすい

---

## ② ブロードキャスト（Broadcasting）

配列の形が違っても、自動でサイズを合わせて計算できる仕組みです。

### 例1：スカラーとの計算

```python
a = np.array([1, 2, 3])

print(a + 10)
```

出力

```text
[11 12 13]
```

内部的には

```text
[1, 2, 3]
+
[10,10,10]
```

のように扱われます。

### 例2：2次元配列

```python
A = np.array([
    [1, 2, 3],
    [4, 5, 6]
])

print(A + 100)
```

出力

```text
[[101 102 103]
 [104 105 106]]
```

---

## ③ 内部が高速（C言語レベル）

NumPyの主要な処理はC言語で実装されています。

```python
a = np.random.rand(1000000)
b = np.random.rand(1000000)

c = a * b
```

100万件以上のデータでも高速に処理できます。

---

# 🔷 普通のPythonとの違い

| 項目 | Pythonリスト | NumPy |
|--------|--------|--------|
| 計算速度 | 遅い | 速い |
| メモリ効率 | 普通 | 良い |
| ベクトル計算 | 不得意 | 得意 |
| 行列計算 | 面倒 | 簡単 |
| AI・データ分析 | 不向き | 必須級 |

---

# 🔷 車業界での使い道（重要）

## ① センサーデータ処理

加速度センサーの平均値計算

```python
acc = np.array([0.1, 0.2, 0.15, 0.18])

avg = np.mean(acc)

print(avg)
```

出力

```text
0.1575
```

---

## ② 車速データ解析

```python
speed = np.array([20, 25, 30, 35, 40])

print(np.max(speed))
```

出力

```text
40
```

最高速度を簡単に取得できます。

---

## ③ カメラ画像処理

画像は数値の集合として扱われます。

```python
image = np.array([
    [100, 120],
    [150, 200]
])

print(image)
```

出力

```text
[[100 120]
 [150 200]]
```

画像認識やADAS開発の基礎になります。

---

## ④ 制動・加速シミュレーション

```python
v = np.array([30, 40, 50])

distance = v ** 2

print(distance)
```

出力

```text
[ 900 1600 2500]
```

複数条件を一括計算できます。

---

# 🔷 重要ポイントまとめ

✅ Pythonの高速な数値計算ライブラリ

✅ 配列（Array）を効率よく扱える

✅ 足し算・掛け算を一括計算できる

✅ ベクトル化によりfor文が不要

✅ ブロードキャストで柔軟な計算が可能

✅ 行列計算・統計計算が得意

✅ AI・機械学習・データ分析の基礎

✅ 車業界では

- センサーデータ処理
- 車速解析
- カメラ画像処理
- 制御シミュレーション

などで幅広く活用される

> NumPyは「Pythonで数値計算を行うための土台」となるライブラリであり、Pandas、Matplotlib、Scikit-learn、TensorFlow、PyTorchなど多くのライブラリの基盤となっています。